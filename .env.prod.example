# ===========================================
# Production Server Environment (EC2 Prod)
# ===========================================
# 사용법:
#   cp .env.prod.example .env.prod
#   docker compose --env-file .env.prod -f docker/docker-compose.prod.yml up -d --build

# ===========================================
# Frontend Apps (Admin, Mobile)
# ===========================================
# Admin: Vercel Production 환경변수에서 설정
# Mobile: EAS Build 환경변수에서 설정
NEXT_PUBLIC_API_URL=https://api.your-domain.com/api
EXPO_PUBLIC_API_URL=https://api.your-domain.com/api

# ===========================================
# API Server
# ===========================================
# Database (Docker 내부 연결)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_TO_VERY_SECURE_PASSWORD
POSTGRES_DB=travel_helper

# JWT (프로덕션용 - 반드시 강력한 값으로 변경!)
# 생성: openssl rand -base64 32
JWT_SECRET=CHANGE_THIS_USE_OPENSSL_RAND_BASE64_32
JWT_REFRESH_SECRET=CHANGE_THIS_USE_OPENSSL_RAND_BASE64_32
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS (프로덕션 프론트엔드 도메인만)
CORS_ORIGIN=https://your-admin.vercel.app

# AI Service
AI_PROVIDER=groq
AI_SERVICE_URL=http://your-ai-service-url:8000
GROQ_API_KEY=gsk_your_groq_api_key

# Queue
QUEUE_PROVIDER=sync

# AWS
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SQS_QUEUE_URL=
AWS_S3_BUCKET=

# External APIs
EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v4/latest

# Social Login
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

APPLE_CLIENT_ID=
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=

# Monitoring
SENTRY_DSN=
