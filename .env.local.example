# ===========================================
# Local Development Environment
# ===========================================
# 사용법:
#   cp .env.local.example .env.local
#   pnpm docker:local   # PostgreSQL 실행
#   pnpm dev            # API 로컬 실행

# Database (로컬 Docker PostgreSQL)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/travel_helper
DIRECT_URL=postgresql://postgres:postgres@localhost:5432/travel_helper

# JWT (로컬용 - 개발 편의를 위한 고정값)
JWT_SECRET=local-dev-secret-key-min-32-characters-here
JWT_REFRESH_SECRET=local-dev-refresh-secret-32-characters
JWT_EXPIRES_IN=1d
JWT_REFRESH_EXPIRES_IN=30d

# CORS (로컬 프론트엔드)
CORS_ORIGIN=http://localhost:3001,http://localhost:19006

# AI Service (로컬 또는 mock)
AI_PROVIDER=groq
AI_SERVICE_URL=http://localhost:8000
GROQ_API_KEY=gsk_your_groq_api_key

# Queue
QUEUE_PROVIDER=sync

# AWS (로컬에서는 비워두거나 테스트용)
AWS_REGION=ap-northeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_SQS_QUEUE_URL=
AWS_S3_BUCKET=

# External APIs
EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v4/latest
