# Travel Helper v2.0 - 종합 코드 리뷰 보고서
> 향후 아키텍처 재설계 시 참고 문서

**작성일**: 2026-01-22
**리뷰 대상**: wigtn-travel-helper v2.0
**총 코드량**: 약 7,041 lines

---

## 1. Executive Summary

### 1.1 프로젝트 현황 요약

Travel Helper v2.0은 해외 여행 중 다중 국가/통화 지출 관리를 위한 React Native(Expo) 앱입니다. 현재 MVP 핵심 기능은 구현 완료되었으며, v2.0 확장 기능(다중 방문지, 환전 지갑)이 진행 중입니다.

### 1.2 주요 강점
- **명확한 아키텍처**: Store/Query/Component 분리가 잘 되어 있음
- **타입 안정성**: TypeScript 활용으로 인터페이스 명확
- **디자인 시스템**: 체계적인 테마 시스템 (색상, 타이포그래피, 스페이싱)
- **오프라인 우선**: SQLite 기반 로컬 데이터 저장

### 1.3 주요 개선점
- **테스트 부재**: 단위/통합 테스트 없음
- **에러 핸들링**: console.error 수준의 기본적인 처리만 존재
- **코드 컨벤션**: ESLint/Prettier 설정 파일 없음
- **CI/CD**: 자동화 파이프라인 없음

### 1.4 프로덕션 준비도 점수

| 영역 | 점수 | 상태 |
|------|------|------|
| 테스트 | 0/10 | ❌ |
| 에러 핸들링 | 5/10 | ⚠️ |
| 보안 설정 | 6/10 | ⚠️ |
| 로깅/모니터링 | 2/10 | ❌ |
| 코드 컨벤션 | 7/10 | ⚠️ |
| CI/CD | 0/10 | ❌ |
| **전체** | **32/70 (46%)** | ⚠️ |

---

## 2. 프로젝트 개요

### 2.1 목적 및 비전
해외 여행 중 지출을 현지 통화로 입력하면 자동으로 원화로 환산하여 관리하는 앱.
환율 계산 없이 빠르게 지출을 기록하고, 여행별/카테고리별 통계를 제공한다.

### 2.2 대상 사용자
- 해외 여행을 자주 가는 20-40대 한국인 여행자
- 여행 중 지출 관리가 필요한 사용자
- 환율 계산이 번거로운 사용자

### 2.3 핵심 기능
1. **여행 관리**: 여행 생성/수정/삭제, 다중 방문지 지원
2. **지출 기록**: 현지 통화 입력, 자동 원화 환산, 카테고리 분류
3. **환전 지갑**: 환전 금액 관리, 잔액 추적
4. **통계**: 카테고리별/통화별/방문지별 지출 분석
5. **오프라인 지원**: 로컬 DB 저장, 캐시된 환율 사용

---

## 3. 기술 스택

### 3.1 핵심 프레임워크

| Layer | Technology | Version | 비고 |
|-------|------------|---------|------|
| Framework | Expo SDK | 54.0.31 | React Native 기반 |
| Language | TypeScript | 5.9.2 | 타입 안정성 |
| UI | React Native | 0.81.5 | 최신 버전 |
| React | React | 19.1.0 | 최신 버전 |
| Navigation | expo-router | 6.0.21 | 파일 기반 라우팅 |
| State | Zustand | 5.0.10 | 경량 상태 관리 |
| DB | expo-sqlite | 16.0.10 | 오프라인 지원 |

### 3.2 주요 라이브러리

| 카테고리 | 라이브러리 | 용도 |
|----------|------------|------|
| UI | @expo/vector-icons | 아이콘 |
| Chart | react-native-chart-kit | 통계 차트 |
| Animation | react-native-reanimated | 애니메이션 |
| Gesture | react-native-gesture-handler | 제스처 처리 |
| Date | @react-native-community/datetimepicker | 날짜 선택 |
| Haptics | expo-haptics | 햅틱 피드백 |

### 3.3 의존성 분석

**장점:**
- Expo 생태계를 잘 활용하여 네이티브 기능 접근 용이
- 최신 버전 라이브러리 사용 (React 19, RN 0.81)
- 경량 상태 관리 (Zustand)로 보일러플레이트 최소화

**주의점:**
- `react-native-chart-kit` (v6.12.0) - 2년 이상 업데이트 없음
- NativeWind(Tailwind) 미사용 - PRD와 불일치

---

## 4. 현재 아키텍처 분석

### 4.1 디렉토리 구조

```
travel-helper/
├── app/                          # Expo Router 페이지
│   ├── (tabs)/                   # 탭 네비게이션
│   │   ├── _layout.tsx           # 탭 레이아웃
│   │   ├── index.tsx             # 홈 (496 lines)
│   │   ├── calendar.tsx          # 캘린더
│   │   ├── stats.tsx             # 통계 (559 lines)
│   │   └── settings.tsx          # 설정
│   ├── expense/
│   │   ├── new.tsx               # 지출 입력 (726 lines)
│   │   └── [id].tsx              # 지출 수정
│   ├── trip/
│   │   ├── new.tsx               # 여행 생성 (471 lines)
│   │   └── [id].tsx              # 여행 상세
│   └── _layout.tsx               # 루트 레이아웃
│
├── components/
│   └── ui/                       # 재사용 UI 컴포넌트
│       ├── AmountDisplay.tsx     # 금액 표시
│       ├── BottomSheet.tsx       # 바텀 시트
│       ├── Button.tsx            # 버튼
│       ├── Card.tsx              # 카드
│       ├── CategoryIcon.tsx      # 카테고리 아이콘
│       ├── EmptyState.tsx        # 빈 상태
│       ├── FAB.tsx               # FAB 버튼
│       ├── Input.tsx             # 입력 필드
│       ├── ProgressBar.tsx       # 진행 바
│       └── index.ts              # 배럴 파일
│
├── lib/
│   ├── api/
│   │   └── exchangeRate.ts       # 환율 API (77 lines)
│   ├── db/
│   │   ├── schema.ts             # DB 스키마 (187 lines)
│   │   └── queries.ts            # DB 쿼리 (373 lines)
│   ├── stores/
│   │   ├── tripStore.ts          # 여행 스토어 (203 lines)
│   │   ├── expenseStore.ts       # 지출 스토어 (163 lines)
│   │   ├── walletStore.ts        # 지갑 스토어 (205 lines)
│   │   └── exchangeRateStore.ts  # 환율 스토어
│   ├── theme/
│   │   ├── colors.ts             # 색상 시스템 (116 lines)
│   │   ├── typography.ts         # 타이포그래피
│   │   ├── spacing.ts            # 스페이싱
│   │   ├── shadows.ts            # 그림자
│   │   ├── animation.ts          # 애니메이션
│   │   └── index.ts              # 테마 훅
│   ├── hooks/
│   │   └── useTheme.ts           # 테마 훅
│   ├── utils/
│   │   ├── currency.ts           # 통화 유틸
│   │   ├── date.ts               # 날짜 유틸
│   │   ├── constants.ts          # 상수 (70 lines)
│   │   └── uuid.ts               # UUID 생성
│   └── types.ts                  # 타입 정의 (117 lines)
│
└── docs/
    ├── prd/
    │   └── PRD_travel-expense-tracker.md
    └── todo_plan/
        └── PLAN_travel-expense-tracker.md
```

### 4.2 모듈 의존성 맵

```
┌─────────────────────────────────────────────────────────────┐
│                         Pages (app/)                        │
│  index.tsx │ stats.tsx │ expense/new.tsx │ trip/new.tsx    │
└──────────────────────────┬──────────────────────────────────┘
                           │
              ┌────────────┼────────────┐
              ▼            ▼            ▼
        ┌─────────┐  ┌──────────┐  ┌──────────┐
        │ Stores  │  │Components│  │  Theme   │
        │(Zustand)│  │   (UI)   │  │ System   │
        └────┬────┘  └──────────┘  └──────────┘
             │
             ▼
    ┌─────────────────┐
    │   DB Queries    │
    │  (queries.ts)   │
    └────────┬────────┘
             │
             ▼
    ┌─────────────────┐
    │   SQLite DB     │
    │  (schema.ts)    │
    └─────────────────┘
```

### 4.3 데이터 흐름

```
User Action
    │
    ▼
Page Component ──▶ Store Action ──▶ DB Query ──▶ SQLite
    │                   │
    │                   ▼
    │              State Update
    │                   │
    ◀───────────────────┘
    │
    ▼
UI Re-render
```

### 4.4 상태 관리 패턴 (Zustand Stores)

| Store | 역할 | 주요 상태 |
|-------|------|----------|
| tripStore | 여행 관리 | trips, activeTrip, activeTrips, destinations |
| expenseStore | 지출 관리 | expenses, isLoading |
| walletStore | 지갑 관리 | wallets, walletBalances |
| exchangeRateStore | 환율 관리 | rates, lastUpdated |

**패턴 특징:**
- 각 스토어가 해당 도메인의 CRUD + 통계 로직 담당
- `getState()` 활용하여 스토어 간 참조 (expenseStore → walletStore)
- 비동기 액션에서 try/catch + console.error 패턴 사용

### 4.5 DB 스키마 구조

```sql
-- 핵심 테이블 관계
trips (1) ──▶ (N) destinations
trips (1) ──▶ (N) wallets
trips (1) ──▶ (N) expenses

wallets (1) ──▶ (N) wallet_transactions
destinations (1) ──▶ (N) expenses (nullable FK)
wallets (1) ──▶ (N) expenses (nullable FK)
```

**테이블 상세:**

| 테이블 | 설명 | 주요 컬럼 |
|--------|------|----------|
| trips | 여행 | id, name, startDate, endDate, budget |
| destinations | 방문지 | tripId, country, city, currency, orderIndex |
| wallets | 환전 지갑 | tripId, currency, name |
| wallet_transactions | 지갑 거래 | walletId, type, amount, exchangeRate |
| expenses | 지출 | tripId, destinationId, amount, currency, amountKRW, category, paymentMethod |
| exchange_rates | 환율 캐시 | rates (JSON), lastUpdated |

---

## 5. 현재 구현 상태

### 5.1 완료된 기능 (PRD 기준)

| ID | 기능 | 상태 | 비고 |
|----|------|------|------|
| FR-001 | 여행 생성 (국가, 기간, 통화) | ✅ | 다중 방문지 지원 추가 |
| FR-002 | 지출 입력 (금액, 카테고리, 메모) | ✅ | 결제 수단, 시간 추가 |
| FR-003 | 환율 API 연동 + 캐싱 | ✅ | 24시간 캐시, 오프라인 폴백 |
| FR-004 | 자동 원화 변환 | ✅ | |
| FR-005 | 캘린더 뷰 | ✅ | |
| FR-006 | 지출 목록/수정/삭제 | ✅ | |
| FR-007 | 카테고리별 지출 통계 | ✅ | 통화별/방문지별 추가 |
| FR-008 | 오프라인 모드 | ✅ | |
| FR-010 | 다중 통화 지원 | ✅ | v2.0 추가 |

### 5.2 진행 중인 기능

| 기능 | 상태 | 비고 |
|------|------|------|
| 환전 지갑 관리 | 🔄 80% | 잔액 추적 구현 완료, UI 개선 필요 |
| 디자인 시스템 적용 | 🔄 70% | 테마 구현 완료, 일부 컴포넌트 미적용 |
| v1→v2 마이그레이션 | ✅ | schema.ts에 구현 |

### 5.3 예정된 기능 (미구현)

| 기능 | 우선순위 | 비고 |
|------|----------|------|
| 영수증 OCR | P3 | receiptImage, ocrProcessed 필드 준비됨 |
| 데이터 백업/복원 | P2 | |
| 여행 요약 리포트 | P2 | |
| 다크모드 토글 | P1 | 시스템 설정 연동됨, 수동 토글 없음 |

---

## 6. 코드 품질 평가

### 6.1 테스트 커버리지

**현황: 0%** ❌

- 단위 테스트 없음
- 통합 테스트 없음
- E2E 테스트 없음
- Jest/Testing Library 설정 없음

**권장 사항:**
1. Jest + React Native Testing Library 설정
2. Store 로직 단위 테스트 우선 작성
3. 핵심 UI 컴포넌트 테스트 추가

### 6.2 에러 핸들링 패턴

**현황: 5/10** ⚠️

**현재 패턴:**
```typescript
// 일반적인 패턴 (stores/)
try {
  const data = await queries.someQuery();
  set({ data, isLoading: false });
} catch (error) {
  console.error('Failed to load:', error);
  set({ isLoading: false });
}
```

**문제점:**
- 사용자에게 에러 피드백 부족
- 에러 복구 로직 없음
- 에러 로깅 시스템 없음 (console만 사용)
- API 실패 시 재시도 로직 없음

**개선점:**
- exchangeRate.ts의 폴백 로직은 잘 구현됨 (캐시 → 기본 환율)

### 6.3 타입 안정성

**현황: 8/10** ✅

**장점:**
- 모든 인터페이스 명확히 정의 (`lib/types.ts`)
- Zustand 스토어 타입 완전히 정의
- DB 쿼리 반환 타입 명시

**개선점:**
- 일부 `any` 타입 사용 (stats.tsx의 `categoryLocalAmounts`)
- 런타임 타입 검증 없음

### 6.4 코드 스타일/컨벤션

**현황: 7/10** ⚠️

**장점:**
- 일관된 파일 구조
- 명확한 네이밍 컨벤션
- 적절한 주석 (한국어)

**문제점:**
- ESLint 설정 파일 없음 (`.eslintrc`)
- Prettier 설정 파일 없음 (`.prettierrc`)
- 일부 파일의 코드량 과다 (expense/new.tsx: 726 lines)

---

## 7. 프로덕션 준비 상태

### 7.1 보안

**현황: 6/10** ⚠️

**장점:**
- 로컬 데이터만 저장 (서버 없음)
- 민감 정보 최소화

**문제점:**
- 환율 API 키 하드코딩 없음 (무료 API 사용)
- 환경 변수 설정 없음 (`.env`)
- 입력 값 검증 미흡 (SQL Injection 가능성 낮지만 검증 없음)

### 7.2 로깅/모니터링

**현황: 2/10** ❌

- `console.log/error`만 사용
- 에러 트래킹 서비스 없음 (Sentry 등)
- 사용자 분석 없음

### 7.3 CI/CD

**현황: 0/10** ❌

- GitHub Actions 없음
- 자동 빌드/테스트 없음
- 배포 자동화 없음

### 7.4 성능 고려사항

**장점:**
- SQLite 인덱스 적절히 설정
- Zustand의 선택적 구독으로 불필요한 리렌더링 방지
- 환율 캐싱 (24시간)

**잠재적 문제:**
- 대량 데이터 시 통계 계산 성능 (메모리 내 처리)
- FlatList 미사용 (ScrollView 사용)

---

## 8. 아키텍처 관점 분석 (재설계 참고)

### 8.1 현재 아키텍처 강점

1. **명확한 레이어 분리**
   - Page → Store → Query → DB 구조 일관성
   - 관심사 분리가 잘 됨

2. **Zustand 활용**
   - 보일러플레이트 최소화
   - 스토어 간 참조 용이

3. **디자인 시스템**
   - 테마 토큰화 (colors, spacing, typography)
   - 다크모드 지원 준비

4. **오프라인 우선**
   - SQLite 로컬 DB
   - 환율 캐시 + 폴백

### 8.2 기술 부채 목록

| 항목 | 심각도 | 설명 |
|------|--------|------|
| 테스트 부재 | 높음 | 리팩토링/기능 추가 시 회귀 위험 |
| 대형 컴포넌트 | 중간 | expense/new.tsx (726 lines) 분할 필요 |
| 에러 핸들링 | 중간 | 사용자 피드백 부족 |
| 환경 변수 | 낮음 | 설정 하드코딩 |
| 코드 컨벤션 | 낮음 | ESLint/Prettier 설정 부재 |

### 8.3 확장성 제한 사항

1. **동기화 기능 추가 시**
   - 현재 오프라인 전용, 서버 동기화 구조 없음
   - 충돌 해결 로직 필요

2. **다국어 지원 시**
   - i18n 구조 없음
   - 문자열 하드코딩

3. **사용자 인증 추가 시**
   - 인증 레이어 부재
   - 토큰 관리 로직 필요

### 8.4 리팩토링 필요 영역

1. **expense/new.tsx 분할**
   ```
   expense/new.tsx (726 lines)
   ├── components/ExpenseAmountInput.tsx
   ├── components/ExpenseCategorySelector.tsx
   ├── components/ExpensePaymentSelector.tsx
   └── hooks/useExpenseForm.ts
   ```

2. **stats.tsx 분할**
   ```
   stats.tsx (559 lines)
   ├── components/StatsSummaryCards.tsx
   ├── components/CategoryChart.tsx
   ├── components/CurrencyBreakdown.tsx
   └── hooks/useExpenseStats.ts
   ```

3. **Store 로직 분리**
   - 통계 계산 로직 → 별도 유틸 함수
   - 복잡한 상태 업데이트 → 리듀서 패턴 고려

### 8.5 모듈 결합도 분석

**낮은 결합도 (Good):**
- components/ui/ - 순수 프레젠테이션 컴포넌트
- lib/theme/ - 독립적인 디자인 시스템
- lib/utils/ - 순수 유틸 함수

**높은 결합도 (Attention):**
- expenseStore ↔ walletStore (지갑 결제 시 직접 호출)
- Pages ↔ 여러 Stores (5개 이상 훅 사용)

**권장:**
- 지갑-지출 연동 로직을 별도 서비스 레이어로 분리
- 페이지 레벨에서 필요한 데이터만 선택적 구독

---

## 9. 권장 사항

### 9.1 즉시 개선 (High Priority)

| 항목 | 작업 | 예상 효과 |
|------|------|----------|
| ESLint/Prettier 설정 | `.eslintrc`, `.prettierrc` 추가 | 코드 일관성 |
| 기본 테스트 추가 | Store 단위 테스트 | 회귀 방지 |
| 에러 처리 개선 | 사용자 피드백 Toast/Alert | UX 개선 |

### 9.2 중기 개선 (Medium Priority)

| 항목 | 작업 | 예상 효과 |
|------|------|----------|
| 대형 파일 분할 | expense/new.tsx, stats.tsx | 유지보수성 |
| CI/CD 설정 | GitHub Actions | 빌드 자동화 |
| 에러 트래킹 | Sentry 연동 | 운영 모니터링 |
| 환경 변수 | .env + expo-constants | 설정 관리 |

### 9.3 아키텍처 개선 제안

1. **서비스 레이어 도입**
   ```
   Pages → Services → Stores → Queries → DB
   ```
   - 복잡한 비즈니스 로직을 서비스로 분리
   - 스토어는 상태 관리에만 집중

2. **Feature 기반 구조 고려**
   ```
   features/
   ├── trip/
   │   ├── components/
   │   ├── hooks/
   │   ├── store.ts
   │   └── queries.ts
   ├── expense/
   └── wallet/
   ```

3. **테스트 전략**
   - 단위: Store, Utils, Services
   - 통합: DB Queries
   - E2E: 핵심 사용자 플로우 (Detox/Maestro)

---

## 10. 부록

### A. 파일별 코드량 (주요 파일)

| 파일 | Lines | 비고 |
|------|-------|------|
| app/expense/new.tsx | 726 | 분할 필요 |
| app/(tabs)/stats.tsx | 559 | 분할 필요 |
| app/(tabs)/index.tsx | 496 | |
| app/trip/new.tsx | 471 | |
| lib/db/queries.ts | 373 | |
| lib/stores/walletStore.ts | 205 | |
| lib/stores/tripStore.ts | 203 | |
| lib/db/schema.ts | 187 | |
| lib/stores/expenseStore.ts | 163 | |
| lib/types.ts | 117 | |
| lib/theme/colors.ts | 116 | |
| **총계** | **~7,041** | |

### B. 주요 함수/컴포넌트 목록

**Stores (Zustand):**
- `useTripStore`: loadTrips, loadActiveTrips, createTrip, updateTrip, deleteTrip, loadDestinations
- `useExpenseStore`: loadExpenses, createExpense, updateExpense, deleteExpense, getStats
- `useWalletStore`: loadWallets, createWallet, addDeposit, addWithdraw, adjustBalance
- `useExchangeRateStore`: loadRates, getRate, convert

**DB Queries:**
- Trips: getAllTrips, getTripById, getActiveTrips, createTrip, updateTrip, deleteTrip
- Destinations: getDestinationsByTripId, getCurrentDestination, createDestination
- Expenses: getExpensesByTripId, createExpense, getTotalExpenseByTrip, getTodayExpenseByTrip
- Wallets: getWalletsByTripId, getWalletBalance, createWalletTransaction
- ExchangeRates: getExchangeRates, saveExchangeRates

**UI Components:**
- Card, Button, Input, FAB, BottomSheet, ProgressBar, CategoryIcon, AmountDisplay, EmptyState

### C. 지원 통화 목록 (18개)

JPY, USD, EUR, GBP, CNY, THB, VND, TWD, PHP, SGD, AUD, CAD, CHF, CZK, HKD, MYR, NZD, IDR

### D. 카테고리 목록 (6개)

| ID | 라벨 | 아이콘 |
|----|------|--------|
| food | 식비 | restaurant |
| transport | 교통 | directions-bus |
| shopping | 쇼핑 | shopping-bag |
| lodging | 숙박 | hotel |
| activity | 관광 | local-activity |
| etc | 기타 | more-horiz |

---

**문서 끝**
